connector.name=postgresql
connection-url=jdbc:postgresql://postgres:5432/statgate_ml_staging
connection-user=${ENV:KAGGLE_DB_USER}
# Password injected at runtime from the environment (fail-closed if unset).
# Do NOT hardcode credentials in this file.
connection-password=${ENV:KAGGLE_DB_PASSWORD}
